import React, { Component } from 'react'

export class nextBtn extends Component {
    render() {
        return (
            <div className="text-center">
                <button className="btn btn-primary text-monospace">Next</button>
            </div>
        )
    }
}

export default nextBtn
